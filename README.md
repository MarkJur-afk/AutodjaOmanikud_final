# 🚗 AutodjaOmanikud - Система управления автосервисом

Современное Windows Forms приложение для управления автосервисом с использованием Entity Framework Core, модульной архитектуры и многоязычной поддержкой.

## 🎯 Особенности

- **Модульная архитектура** - отдельные UserControl для каждой функции
- **Entity Framework Core** - современный ORM с Code First подходом
- **SQLite база данных** - легкая и надежная
- **Многоязычность** - поддержка русского и эстонского языков
- **Профессиональный UI** - современный дизайн с эмодзи
- **Автоматические обновления** - синхронизация данных между модулями

## 🏗️ Архитектура

```
📁 AutodjaOmanikud/
├── 📁 Controls/           # Модульные пользовательские контролы
│   ├── OwnerControl.cs    # Управление владельцами
│   ├── CarControl.cs      # Управление автомобилями
│   ├── ServiceControl.cs  # Управление обслуживанием
│   └── ServiceTypeControl.cs # Управление типами услуг
├── 📁 Data/               # Контекст Entity Framework
├── 📁 Models/             # Модели данных
├── 📁 Services/           # Бизнес-логика
├── 📁 Migrations/         # Миграции базы данных
├── Localization.cs        # Система локализации
└── Form1.cs               # Главная форма
```

## 🌍 Многоязычность

Приложение поддерживает два языка:
- **Русский** - основной язык интерфейса
- **Эстонский** - полный перевод всех элементов

### Переключение языка
- Кнопка **RU/ET** в правом верхнем углу
- Мгновенное обновление всего интерфейса
- Сохранение выбранного языка в сессии

## 🚀 Технологии

- **C# .NET 8.0** - современная платформа разработки
- **Windows Forms** - нативный UI для Windows
- **Entity Framework Core 8.0** - ORM для работы с базой данных
- **SQLite** - встроенная база данных
- **Visual Studio 2022** - IDE для разработки

## 📊 Модели данных

### Owner (Владелец)
```csharp
public class Owner
{
    public int Id { get; set; }
    public string FullName { get; set; }    // Полное имя
    public string Phone { get; set; }       // Телефон
    public ICollection<Car> Cars { get; set; } // Автомобили
}
```

### Car (Автомобиль)
```csharp
public class Car
{
    public int Id { get; set; }
    public string Brand { get; set; }           // Марка
    public string Model { get; set; }           // Модель
    public string RegistrationNumber { get; set; } // Гос. номер
    public int OwnerId { get; set; }            // ID владельца
    public Owner Owner { get; set; }            // Владелец
    public ICollection<Service> Services { get; set; } // Услуги
}
```

### ServiceType (Тип услуги)
```csharp
public class ServiceType
{
    public int Id { get; set; }
    public string Name { get; set; }        // Название услуги
    public decimal Price { get; set; }      // Цена
    public ICollection<Service> Services { get; set; } // Записи обслуживания
}
```

### Service (Запись обслуживания)
```csharp
public class Service
{
    public int Id { get; set; }
    public int CarId { get; set; }          // ID автомобиля
    public int ServiceTypeId { get; set; }  // ID типа услуги
    public DateTime Time { get; set; }      // Время обслуживания
    public bool IsPaid { get; set; }        // Оплачено ли
    public Car Car { get; set; }            // Автомобиль
    public ServiceType ServiceType { get; set; } // Тип услуги
}
```

## 🔧 Установка и запуск

### Требования
- Windows 10/11
- .NET 8.0 Runtime
- Visual Studio 2022 (для разработки)

### Шаги установки

1. **Клонирование репозитория**
```bash
git clone https://github.com/[username]/AutodjaOmanikud.git
cd AutodjaOmanikud
```

2. **Восстановление пакетов**
```bash
dotnet restore
```

3. **Применение миграций**
```bash
dotnet ef database update
```

4. **Запуск приложения**
```bash
dotnet run
```

## 📱 Использование

### 👥 Управление владельцами
1. Откройте вкладку "Владельцы" / "Omanikud"
2. Введите имя и телефон
3. Нажмите "Добавить" / "Lisa"
4. Для редактирования выберите строку и нажмите "Изменить" / "Muuda"
5. Для удаления выберите строку и нажмите "Удалить" / "Kustuta"

### 🚗 Управление автомобилями
1. Откройте вкладку "Автомобили" / "Autod"
2. Выберите владельца из списка
3. Введите марку, модель и гос. номер
4. Нажмите "Добавить" / "Lisa"

### 🔧 Управление обслуживанием
1. Откройте вкладку "Обслуживание" / "Hooldus"
2. Выберите автомобиль и тип услуги
3. Установите дату и статус оплаты
4. Нажмите "Добавить" / "Lisa"
5. Для изменения статуса оплаты используйте кнопку "Изменить статус оплаты"

### ⚙️ Управление услугами
1. Откройте вкладку "Услуги" / "Teenused"
2. Введите название услуги и цену
3. Нажмите "Добавить" / "Lisa"
4. Для редактирования выберите строку и нажмите "Изменить" / "Muuda"

### 📊 Статистика
Статистика отображается в заголовке окна:
- Количество клиентов / владельцев
- Количество автомобилей
- Количество услуг
- Общий доход в евро

## 🏆 Преимущества модульной архитектуры

- **Разделение ответственности** - каждый контрол отвечает за свою область
- **Переиспользование кода** - контролы можно использовать в других формах
- **Легкость тестирования** - каждый модуль тестируется отдельно
- **Масштабируемость** - легко добавлять новые функции
- **Поддержка** - проще находить и исправлять ошибки
- **Локализация** - централизованная система переводов

## 🔄 Система событий

Контролы используют события для синхронизации данных:

```csharp
// В контроле
public event Action DataChanged;

// При изменении данных
DataChanged?.Invoke();

// В главной форме
ownerControl.DataChanged += OnDataChanged;
```

## 🌍 Система локализации

```csharp
public static class Localization
{
    public static string CurrentLanguage { get; set; } = "ru";
    
    public static string GetString(string key)
    {
        return CurrentLanguage switch
        {
            "et" => GetEstonian(key),
            "ru" => GetRussian(key),
            _ => GetRussian(key)
        };
    }
}
```

## 🛠️ Разработка

### Добавление нового контрола

1. Создайте новый UserControl в папке `Controls/`
2. Реализуйте интерфейс с событием `DataChanged`
3. Добавьте метод `UpdateLocalization()`
4. Добавьте контрол на главную форму
5. Подпишитесь на событие изменения данных

### Структура контрола
```csharp
public partial class NewControl : UserControl
{
    private AutoDbContext _context;
    public event Action DataChanged;

    public NewControl()
    {
        InitializeComponent();
        _context = new AutoDbContext();
        LoadData();
    }

    private void OnDataModified()
    {
        DataChanged?.Invoke();
    }

    public void RefreshData() => LoadData();
    
    public void UpdateLocalization()
    {
        // Обновление текстов элементов интерфейса
        buttonAdd.Text = Localization.GetString("Add");
        buttonEdit.Text = Localization.GetString("Edit");
        buttonDelete.Text = Localization.GetString("Delete");
    }
}
```

### Добавление нового перевода

1. Откройте файл `Localization.cs`
2. Добавьте новый ключ в методы `GetRussian()` и `GetEstonian()`
3. Используйте `Localization.GetString("YourKey")` в коде

## 📈 Планы развития

- [ ] Добавить поддержку английского языка
- [ ] Реализовать систему отчетов
- [ ] Добавить поиск и фильтрацию
- [ ] Создать систему резервного копирования
- [ ] Добавить экспорт в Excel/PDF
- [ ] Реализовать многопользовательский режим
- [ ] Добавить уведомления о предстоящем обслуживании

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request


## 👨💻 Автор

**[Ваше имя]**
- GitHub: [@yourusername](https://github.com/MarkJur-afk)
- Email: markjurgennn@gmail.com

## 🙏 Благодарности

- Microsoft за .NET и Entity Framework Core
- Сообщество разработчиков за вдохновение и поддержку
- Все, кто тестировал и предлагал улучшения

---

⭐ Поставьте звезду, если проект был полезен!
